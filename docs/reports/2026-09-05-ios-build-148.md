# SignalQuest iOS 1.0 (148) — préparation locale App Store

**Artefact initial remplacé après correction de la chauffe iPerf3.** Utiliser l’IPA et les preuves du [suivi du correctif de chauffe](2026-09-05-speedtest-ios-warmup-followup.md). Les informations ci-dessous décrivent l’archive initiale conservée pour traçabilité.

## Périmètre

- Worktree `codex/ios-speedtest-v6`, base identique à `origin/main` au démarrage de la préparation.
- `project.yml` passé de 147 à 148 pour l'app, le service de notification et le widget ; `SignalQuest.xcodeproj/project.pbxproj` régénéré avec XcodeGen.
- Certificat Apple Distribution et profils App Store existants sélectionnés. Aucun certificat/profil créé ou révoqué ; les profils locaux existants ont été copiés dans le dossier attendu par Xcode lorsque nécessaire.
- Signature manuelle appliquée uniquement via un xcconfig temporaire. Les préférences de signature du projet restent inchangées.
- Aucun commit, push, upload ni modification App Store Connect effectué par cette tâche.

## Artefacts produits

Dossier : `/tmp/sq-speedtest-v6-ios-148/`

- Archive : `SignalQuest-1.0-148.xcarchive` — archive Release réussie, code retour 0.
- IPA App Store Connect : `export/SignalQuest.ipa` — export réussi, code retour 0 ; destination locale `export`, aucun upload.
- `Signing.xcconfig`, `ExportOptions.plist` : réglages locaux d'archive/export, gestion automatique du numéro de build désactivée.
- `source-manifest.json` : base Git et empreintes SHA256 des fichiers modifiés/ajoutés hors rapports.
- `archive.log` : journal d'archive.

## Vérifications préalables terminées

- Les trois cibles applicatives annoncent `CFBundleShortVersionString = 1.0` et `CFBundleVersion = 148` dans les réglages Release résolus.
- Les trois profils App Store sont valides et correspondent à une identité Apple Distribution disposant de sa clé privée locale.
- Le profil principal autorise `get-task-allow = false`, APNs `production`, les capacités CarPlay/navigation et l'accès Wi-Fi requis.

## Vérifications finales terminées

Les trois bundles de l'archive puis les trois bundles extraits de l'IPA ont été contrôlés indépendamment :

| Bundle | Version / build | Signature | get-task-allow | APNs |
|---|---|---|---|---|
| `fr.signalquest.ios` | 1.0 / 148 | Apple Distribution | false | production |
| `fr.signalquest.ios.notification-service` | 1.0 / 148 | Apple Distribution | false | non applicable |
| `fr.signalquest.ios.widget` | 1.0 / 148 | Apple Distribution | false | non applicable |

- `codesign --verify --deep --strict` réussi pour chacun ; certificats extraits du code signé identiques à l'identité de distribution sélectionnée.
- Profils App Store sans liste d'appareils ni `ProvisionsAllDevices`, valides jusqu'au 4 septembre 2027 ; identifiants signés et profils cohérents.
- Architecture : `arm64` ; OS minimum : `16.0` ; SDK : `iphoneos27.0`.
- Trois dSYMs conservés dans l'archive ; UUID du dSYM principal identique au binaire : `EEDE0503-DD29-318A-A9D3-AD85F1E079DB`.
- Les empreintes du manifeste source correspondent toujours aux fichiers après l'archive.

### Empreinte IPA

Taille : 20925550 octets.

SHA256 : `30e68371f840b9f58c09f8592ad4497340f99f8822560705b6eb756e6ee1a045`.

### Preuves locales

- `archive-verification/verification.json` et `export-verification/verification.json` : signature et entitlements normalisés, sans secret.
- `archive-build-proof.json` : architecture, SDK, dSYM et correspondance du manifeste source.
- `ipa-proof.json` : chemin, taille, SHA256 et absence d'upload.
- `archive.log`, `export.log` : résultats des commandes Xcode.

Verdict : **artefact App Store Connect prêt localement**. La disponibilité de la build dans App Store Connect, la validation de l'upload et la distribution TestFlight restent à la charge de la tâche principale ; aucune de ces actions n'est déclarée effectuée ici.
